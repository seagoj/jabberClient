# jabberClient
Client for displaying messages from Jabber in the console

## JSON gem installation
Run the following in the commandline:

    gem install json
    
## Configuration
Modify the config/config.json file to store the connection credentials to the jabber server.

    {
        "user": "user1234",
        "pass": "secretPassword"
    }
    

